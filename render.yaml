services:
  - type: web
    name: promptyy
    env: static
    buildCommand: pnpm install --frozen-lockfile && pnpm run build
    publishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html